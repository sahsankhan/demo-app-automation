# Xray Cloud / Server configuration
# Set these as CI secrets: XRAY_CLIENT_ID, XRAY_CLIENT_SECRET, JIRA_BASE_URL

projectKey=BANK
testPlanKey=BANK-TP-1
testExecutionKey=

# JUnit import settings
junitImport:
  # Prefix test names with Xray keys for traceability, e.g. "@BANK-101 Registration flow"
  testKeyPattern: "@([A-Z]+-[0-9]+)"
  updateExisting: true
  createMissingTests: false
